import React, { Fragment } from 'react'
import { Head, VideoModal, Ticker } from './../components'

export default props => 
  <Fragment>
    <Head title={`Artists`} />
    <VideoModal/>
    <h1>Artists</h1>
  </Fragment>
